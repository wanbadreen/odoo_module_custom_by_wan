<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_order_form_loyalty_partial_redeem" model="ir.ui.view">
        <field name="name">sale.order.form.loyalty.partial.redeem</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_open_loyalty_redeem_wizard"
                        type="object"
                        string="Redeem Loyalty Points"
                        class="btn btn-secondary"
                        attrs="{'invisible': [('has_loyalty_redeem', '=', True)]}"/>
            </xpath>
        </field>
    </record>
</odoo>